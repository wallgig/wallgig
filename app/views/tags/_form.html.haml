= simple_form_for(@tag) do |f|
  .form-inputs
    = f.input :name
    = f.association :category, collection: @categories, label_method: :name_for_selects, value_method: :id, include_blank: true
    = f.input :purity

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
